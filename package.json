{
  "dependencies": {
    "esbuild": "^0.14.38",
    "purescript": "^0.15.0",
    "sass": "^1.43.4",
    "spago": "^0.20.8"
  },
  "scripts": {
    "build": "spago bundle-app --minify --to dist/bundle.js",
    "buildcss": "sass src/style.scss dist/bundle.css",
    "deploy:test": "scp -r dist/* gbagan@connect.liris.cnrs.fr:~/machine/",
    "deploy": "scp -r dist/* mam@connect.liris.cnrs.fr:~/public_html/machine/"
  }
}
